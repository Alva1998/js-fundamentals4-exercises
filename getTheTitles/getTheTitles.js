const getTheTitles = function(books) {
    return books.map((book) => `${book.title}`); //returns an array of titles
}

module.exports = getTheTitles;

//*the odin project example 
//basically the same thing 
